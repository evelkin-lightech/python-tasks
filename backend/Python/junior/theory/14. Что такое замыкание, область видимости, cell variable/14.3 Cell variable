Cell variable или Objects Cell используются для реализации переменных,
на которые ссылаются несколько областей.

Для каждой такой переменной создается объект cell для хранения значения;
локальные переменные каждого фрейма стека, который ссылается на значение,
содержат ссылку на ячейки из внешних областей, которые также используют эту
переменную.

При обращении к значению вместо самого объекта ячейки используется значение,
содержащееся в ячейке. Это разыменование объекта cell требует поддержки
со стороны сгенерированного байт-кода; ссылки на них не удаляются автоматически
при обращении.

Используется Си-шная структура для cell objects.
