Методы для обработки множеств:
- len (количество элементов в множестве)
      x = { 1, 3, 'abcd', 2.85 }
      len(x)
- isdisjoint (определение отсутствия общих элементов в множествах)
      x = { 2, 3, 4 }
      y = { 7 }
      x.isdisjoint(y) => True
- copy (выполняет копирование множеств)
      x = { 1, 3, 8 }
      y = x.copy()
- update (обновляет множество, добавляя к ней новые элементы)
      x = { 5, 6 }
      x.update({2, 8, 10})
- intersection_update (выполняет обновление текущего множества с
  помощью пересечения множеств). Данная функция выполняет операцию &=
      x = { 1, 2, 3, 4, 5, 6, 7 }
      x.intersection_update({3,4,5})
- difference_update (реализует обновление множества с помощью операции
  вычитания множеств). Функция заменяет операцию –=
      x = { 1, 2, 3, 4, 5, 6, 7, 8, 9}
      y = { 2, 4, 6, 8 }
      x.difference_update(y)
- add (добавляет элемент в множество)
      y.add(5)
- remove (удаляет элемент из множества)
      y.remove(1)
- discard (удаляет элмент без генерации ошибки в случае его отстутствия)
      x.discard(3)
- pop (вытягивает элемент из множества)
      x.pop()
- clear (отчищает множество)
      x.clear()
- union (объединение множеств) - так же оператор |
      x = { 1, 2, 3, 4 }
      y = { 3, 5, 7 }
      z = x.union(y)
- intersection (пересчечение множеств) - так же оператор &
      x = { 1, 2, 3, 4 }
      y = { 3, 5, 7 }
      z = x.intersection(y)
- difference (разность множеств) - так же оператор - (минус)
      x = { 1, 2, 3, 5 }
      y = { 2, 8 }
      z = x.difference(y) => {1, 3, 5}
- symmetric_difference (симметрическая разность множеств) - так же оператор ^
      x = { 1, 2, 3, 5 }
      y = { 2, 8 }
      z = x.symmetric_difference(y)
- set() - создает множество
      x = set(['a', 5, "textbook", 5])
      y = set('Hello!')
      z = set([0,1,2,3,4,5])
- frozenset() - позволяет получить неизменяемое множество
      x = frozenset([1, 2, 3, 4])

Поддерживаются следующие операции над множествами:
- in / not in – проверка элемента на вхождение в множество
      x = { 3, 8, 'а', 'Hello' }
      a = 'Hello'
      if a in x:...
      if a not in x:...
– - (минус) – разность множеств (создается новое множество,
    содержащие элементы множества x, которых нет в множестве y)
      x = { 1, 2, 3, 5 }
      y = { 2, 8 }
      z = x - y => {1, 3, 5}
| – объединение множеств (содержит уникальные элементы всех множеств)
      x = { 1, 2, 3, 4 }
      y = { 3, 5, 7 }
      z = x | y => {1, 2, 4, 5, 7}
& – пересечение множеств (элементы, которые входят только в одно из множеств)
      x = { 1, 2, 3, 4 }
      y = { 3, 5, 7 }
      z = x & y => { 3 }
^ – симметричная разность (обратная операция к операции пересечения множеств)
      x = { 1, 3, 5 }
      y = { 2, 3, 8 }
      z = x ^ y => { 1, 2, 5, 8 }

Операции могут образовывать цепи:
    x | y | z
    x & y & z
    x - y - z
    x ^ y ^ z
