Типы данных Python можно классифицировать на:

- изменяемые типы данных
- неизменяемые типы данных

К изменяемым типам данных можно отнести:
- списки
- словари
- множества

К неизменяемым типам данных относятся:
- числа
- строки
- кортежи
- логические значния

Что подразумевается под изменяемым - неизменяемым типом данных?
При объявлении переменной в компьютере выделеяется область памяти
для хранения значения этой самой переменной.
При попытке внести какие-то либо изменения в переменную, например добавить
новые значения в изменяемый тип данных - его ссылка на область памяти
останется без изменения, а для неизменяемых типов данных наблюдается
обратное поведение - будет создана новая область памяти, которая будет содержать
измененные значения.

Пример:
    a = [1,2,3]
    b = (1,2,3)
    print(id(a))
    print(id(b))

    a += [3,4,5]
    b += (4,5,6)
    print(id(a))
    print(id(b))
